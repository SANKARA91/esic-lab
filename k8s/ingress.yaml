apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: esic-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: esic-app-svc
            port:
              number: 80